## BarKit 1.6.4 (25)

- 本地部署的配置现在只有一个来源

  `codex -p <模型ID>` 用的档案过去是手工文件，在设置里改了地址它不会跟着动，于是「切到本地部署不生效」。这份档案改由 BarKit 生成，和 `config.toml` 的 `barkit_local` 段、`models.json` 的条目同源；设置页新增「实际生效位置」，逐条显示三个文件当前的值，不一致的那一行会标出来。

- Claude 的本地部署入口移除

  Claude Desktop 要求网关是 https（或 loopback）且模型名必须是 `claude-*`，接入明文 HTTP 局域网端点只会得到一堆看不出原因的报错。菜单、Claude Code CLI 和 Desktop 三处现在都会明确拒绝，而不是写一份客户端会把模型删掉的配置。

- Ecarx Claude 适配器随 App 一起安装，并能接管自己遗留的进程

  适配器脚本打进 App，第一次切 Ecarx 自动装好，不再依赖机器上事先存在的 Skill。切换前先检查端口：只接管 BarKit 自己遗留的适配器进程，别的程序占着端口会报错而不是被误杀。

- 「测试连接」真的测试接口

  从「端口能不能连」改为请求 `/models`，能区分接口正常、端口通但接口报错、API Key 被拒、路径少写或多写 `/v1`。以前端口通就显示「连接正常」，正是 500 错误被当成健康的原因。

- 切换配置后不再被旧工作空间导入覆盖

  工作空间合并完成后会记录标记，旧会话导入不会再往已经合并好的默认目录里回灌一份。

- 配置文件加了版本号，损坏或更高版本的文件不会被降级改写

  `agent-secrets.json` 与 `local-deployments.json` 都带 `schemaVersion`：旧文件自动升级，来自更新版本 BarKit 的文件会被拒绝并说明原因。

- 清理了已失效的开关

  Codex 0.142 已移除 `wire_api = "chat"`，本地部署界面里的这个选项删掉了，配置固定写 Responses。

已知边界：如果某次本地部署请求已经把上下文撑过服务端上限（服务端报 `ContextWindowExceededError`），那个历史会话会一直失败，需要在网关侧做超长请求裁剪；新开会话不受影响。

系统要求：macOS 13 或更高版本。
