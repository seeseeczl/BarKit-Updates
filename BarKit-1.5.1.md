## BarKit 1.5.1 (9)

- 修复配置识别：Claude Desktop 的 3P 配置现在会按"实际指向哪"显示厂商，而不是一律标成"其他 3P 配置／官方登陆"。例如本机适配器 `127.0.0.1:61327` 会识别为 Ecarx（外部管理）
- Codex 侧同样处理：不在内置列表里的 provider id 会显示原始 id 并标记为外部配置，不再被误报成官方登录；`config.toml` 读不出来时也会明确报错，而不是沿用旧状态显示"已同步"
- 菜单精简：下拉框已经能表达的状态不再占用副行，只有"CLI 指向别处"或"配置不可读"这类信息才会显示第二行
- 发布链路改为 CI 签名：更新包由 GitHub Actions 构建、Developer ID 签名、Apple 公证，并在发布前校验签名与 App 内置公钥一致、发布后重新下载复验

注意：这一版更换了 Sparkle 更新签名密钥，**已安装 1.5.0 及更早版本的用户需要手动安装本版本一次**；从 1.5.1 起自动更新恢复。

系统要求：macOS 13 或更高版本。
