## BarKit 1.5.4 (20)

- 打开设置不再要求输入钥匙串密码

  以前每打开一次设置，都要按顺序输入三次登录钥匙串密码（DeepSeek / GLM / Ecarx 各一次）：设置页为了显示“已保存”状态会把三个 Key 都读出来，而读取内容要走钥匙串的访问控制；本地开发版用的是临时签名，每次重新编译签名指纹都会变，之前点过的“始终允许”随之失效，所以每次都要重输。现在设置页只检查“是否已保存”、不读取内容，打开设置不再弹密码。输入框也不再回显已保存的 Key，改为提示“已保存到钥匙串（不回显）；粘贴新 Key 可替换”，Key 内容只在切换服务商、真正写入配置时才读取。

- 面板列出的服务器跟随 Finder 实际挂载

  服务器列表以前只在启动时和“设置 > 从 Finder 刷新当前挂载服务器”时更新，运行期间手动挂载的服务器不会出现在面板里，会出现“Finder 里挂了两台、面板只显示一台，点立即连接还提示所有服务器均已挂载”的情况。现在点「立即连接」或登录自动挂载时，会先把 Finder 当前挂载的服务器并入列表；并且只增不减，不会丢掉尚未挂载的条目。

- Harness 安装确认改为面板内提示

  以前用独立弹窗确认安装 Harness，弹窗会让菜单栏面板失去焦点、落到其他窗口后面，安装进度就看不见了。现在确认直接显示在 Harness 行下方，安装期间面板保持在最前，结束后恢复。

系统要求：macOS 13 或更高版本。Harness 安装需要本机有 Node.js / npm。
