## BarKit 1.6.1 (22)

- 修复切换模型配置后历史会话频繁闪烁

  Codex 会按会话创建时记录的 provider 恢复历史对话。此前切到 GLM 时，配置写入会删除 DeepSeek provider 段，打开 DeepSeek 会话就会持续报 `Model provider 'deepseek' not found` 并反复重试。现在切换只替换当前 provider，其他已配置 provider 会保留，历史会话可以直接打开。

- 修复主工作空间的项目归属与旧配置迁移

  旧工作空间的项目、会话归属、顺序和 host 映射会合并到主工作空间；旧 `~/.codex` 官方基线缺失时，第三方模型配置仍可正常创建。

- 修复共享 skills/plugins 的自引用备份

  当共享目录本来就指向用户目录时，不再创建指向自身的备份链接，避免保存失败和递归扫描。

- 统一“模型配置”文案

  菜单和设置页不再称为“工作空间”，避免把新架构误解为切换会话目录。

系统要求：macOS 13 或更高版本。
